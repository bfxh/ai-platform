{
  "name": "uemcp-server",
  "version": "3.8.0",
  "description": "Unreal Engine Model Context Protocol Server",
  "engines": {
    "node": ">=20.19.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "test": "npm run lint && npm run typecheck && npm run test:unit && npm run test:python",
    "test:unit": "jest --config jest.config.unit.js",
    "test:python": "cd tests/python && python -m pytest -v",
    "test:python:coverage": "cd tests/python && python -m pytest --cov=ops --cov=utils --cov-report=term-missing --cov-report=html:coverage/python",
    "test:integration:old": "jest --config jest.config.js tests/tools/ tests/services/",
    "test:coverage": "npm run lint && npm run typecheck && npm run test:unit -- --coverage && npm run test:python:coverage",
    "test:watch": "jest --config jest.config.unit.js --watch",
    "version:bump": "node ../scripts/bump-version.js",
    "version:major": "node ../scripts/bump-version.js --type=major",
    "version:minor": "node ../scripts/bump-version.js --type=minor",
    "version:patch": "node ../scripts/bump-version.js --type=patch",
    "version:beta": "node ../scripts/bump-version.js --prerelease=beta",
    "version:alpha": "node ../scripts/bump-version.js --prerelease=alpha"
  },
  "keywords": [
    "mcp",
    "unreal-engine",
    "ai-assistant"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.10",
    "@typescript-eslint/eslint-plugin": "^8.5.0",
    "@typescript-eslint/parser": "^8.5.0",
    "eslint": "^8.57.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.16.0",
    "typescript": "^5.8.0"
  }
}
