# Development dependencies for local development with Unreal Engine
# For CI/CD, use requirements-ci.txt instead
# Note: The 'unreal' module is provided by Unreal Engine's Python environment
# These are optional dependencies for development and testing

# Testing (optional - for running tests locally)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Development tools (optional - for linting and type checking)
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
isort>=5.12.0
ruff>=0.1.0

# Type stubs (optional - for better IDE support)
types-requests>=2.28.0

# Note: Most functionality works without these dependencies
# They are primarily for development, testing, and code quality