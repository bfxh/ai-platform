# CI/CD dependencies - excludes unreal package which is only available in UE environment

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Linting and type checking
ruff>=0.1.0
mypy>=1.0.0
